@using Microsoft.AspNet.Identity

@using AllServiceProvider.Models
@if (Request.IsAuthenticated)
{
            string profile = Session["profile"] as string;
        using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
        {
    @Html.AntiForgeryToken()

    <ul class="nav navbar-nav" style="width:292%;margin-top:-12%;height:100%;">
        @*<li><a href="javascript:document.getElementById('logoutForm').submit()" class="btn btn-success" style="margin-top:34%;padding:10%; margin-right:4%;margin-left:4%; font-weight:bold;">Log off</a></li>*@
        @*<li style="float:right;">
            @Html.ActionLink("Hello " + User.Identity.GetUserName() +  "", "", routeValues: null, htmlAttributes: new { title = "Goto Homepage" })
        </li>*@
        <li id="user-dropdown" style="margin-left:63%;">
            <div class="a1 dropdown-toggle btn-success" data-toggle="dropdown" style="height:112px; width:180px; margin-left:0%;padding-top:20%;border-radius:7px;">
                <a href="#" style="padding:7%;color:white;margin-top:15px;font-weight:bold;">
                    <span class="glyphicon glyphicon-user"></span>@UserHelper.Name @*@System.Web.HttpContext.Current.Session["Name"]*@<span>  </span>{{name}} <p> 
                </a>
            </div>
            <ul class="dropdown-menu" style="padding:6%;width:100%; border-radius:7px; background-color:lightgreen;position:relative;margin-left:0%;margin-top:-48%;">
                <li class="user-header">
                    <p> @UserHelper.Name@*@System.Web.HttpContext.Current.Session["Name"]*@</p><p>{{name}}</p>
                    @*<p>Member since Aug,2016</p>*@
                </li>
                <li class="user-footer">
                    <div class="pull-left">
                        @Html.ActionLink("Profile", UserHelper.Profile, "Site",new { authUserId = User.Identity.GetUserId() },  htmlAttributes: new { @class = "btn btn-default btn-flat" })
                    @*<a href="#" class="btn btn-default btn-flat">Profile</a>*@
                    </div>
                    <div class="pull-right">
                        <a href="javascript:document.getElementById('logoutForm').submit()"  class="btn btn-default btn-flat">Sign out</a>
                    </div>
                </li>
            </ul>
            <!-- Menu Toggle Button -->
            @*<a href="#" class="dropdown-toggle btn-success" data-toggle="dropdown" style="height:100px; border:solid;">
                <span class="glyphicon glyphicon-user"></span> Asif Sharif Shahid
            </a>
            <ul class="dropdown-menu" style="margin-top:0%; padding:5%;width:100%;">
                <li class="user-header">
                    <p>Asif Sharif Shahid</p>
                    <p>Member since Aug,2016</p>
                </li>
                <li class="user-footer">
                    <div class="pull-left">
                        <a href="#" class="btn btn-default btn-flat">Profile</a>
                    </div>
                    <div class="pull-right">
                        <a href="#" class="btn btn-default btn-flat">Sign out</a>
                    </div>
                </li>
            </ul>*@
        </li>
    </ul>
    }
}
else
{
    <ul class="nav navbar-nav" style="width:105%;">
        <li class="btn btn-success" style="font-weight:bolder;margin-left:1%;">
        
            @Html.ActionLink("Register", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink" })
        </li>
        
        <li class="btn btn-success" style="font-weight:bolder;margin-left:2%;">@Html.ActionLink("Log in", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
    </ul>
}







